import datetime
import requests
from telegram.ext import Application, CommandHandler, ContextTypes, MessageHandler, filters
import time
import csv
import os


async def main(context: ContextTypes.DEFAULT_TYPE):
    return None

    return alerts, heartbeat_messages
